# vendored Box3D clone + build (restored with `jolt box3d`)
vendor/

# built shim (rebuilt with `jolt native`)
native/*.dylib
native/*.so
native/*.so.*
target/
node_modules/
__pycache__/

# tool caches
.jolt/
.clj-kondo/
.lsp/
.nrepl-port

# os
.DS_Store

# scratch
fast-volume.html
naval-telemetry/
